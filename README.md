# Vive Delivery Service

Full-stack project with React frontend and Express backend.

## Setup
1. Backend: `cd backend && npm install && npm start` (ensure MongoDB is running).
2. Frontend: `cd frontend && npm install && npm run dev`.
